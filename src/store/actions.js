const actions = {
  // user_signin: ({ commit }, user) => commit('user_signin')
  changeShowTabbar: ({ commit }) => commit('changeShowTabbar'),
  changeLoading: ({ commit }) => commit('changeLoading')
}

export default actions
